name: python-uv-fixture

steps:
  lint:
    run: bash scripts/fake-ruff.sh {files}
    files: "src/**/*.py"
  test:
    run: bash scripts/fake-pytest.sh {files}
    files: "src/**/*.py"

pipelines:
  ci:
    steps: [lint, test]
  pre-commit:
    steps: [lint]

git:
  hooks:
    pre-commit:
      pipeline: pre-commit
